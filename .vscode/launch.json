{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB: Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/simulation",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "preLaunchTask": "Build"
    }
  ]
}